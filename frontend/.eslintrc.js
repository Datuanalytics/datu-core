module.exports = {
    root: true,
    extends: ['react-app'],
    rules: {
      // You can override rules here, for example:
      'react-hooks/exhaustive-deps': 'warn',
      "import/no-unresolved": "off", // Suppress unresolved import errors

    }
  };
